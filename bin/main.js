#!/usr/bin/env node

/**
 *  defining `eodash.config.js` configuration.
 *  helper function that provides intellisense
 *  without the need for JSDOC:
 * @param {import('./types').EodashConfig} config
 */
export const defineConfig = (config) => {
  return config
}

